import React from "react";
import { Button } from "@/components";

export const PanelLogin = () => {
  return (
    <div>
      <Button func={() => console.log("XD")} type="submit">
        Log in
      </Button>
    </div>
  );
};
